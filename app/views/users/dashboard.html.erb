
<div class="container">
   <h1 class="mt-3">My Dashboard</h1>
  <h2 class="mt-3">My listings</h2>
  <div class="cards">
    <ul>
     <% @experiences.each do |experience| %>
     <% if experience.owner == current_user %>
        <li>
          <div class="card">
            <% if experience.photo? %>
              <%= cl_image_tag experience.photo, height: 180, width: 290, class: 'card-img-top', crop: :thumb %>
              <% else %>
              <%= cl_image_tag experience.picture_url, height: 180, width: 290, class: 'card-img-top', crop: :thumb %>
              <% end %>
            <div class="card-body">
              <h5 class="card-title"><%= experience.title %></h5>
              <p class="card-text"><%= experience.description %></p>
              <p class="text-muted card-body-bottom"><i class="fas fa-dollar-sign"></i><%= experience.price %></p>
            </div>
            <div class="card-footer text-muted text-left">
              <%= link_to 'More info', experience_path(experience)  %>
            </div>
          </div>
        </li>
      <% end %>
      <% end %>
    </ul>
  </div>
